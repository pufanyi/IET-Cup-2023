#include <iostream>
#include <algorithm>

constexpr int maxq = 1000005;
constexpr int maxn = 1000005;

struct Query {
    int l, id, nxt;
} q[maxq];

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
    return 0;
}